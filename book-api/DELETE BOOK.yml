info:
  name: DELETE BOOK
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:3000/books/:id
  params:
    - name: id
      value: "14"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
