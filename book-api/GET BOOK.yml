info:
  name: GET BOOK
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/books/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
