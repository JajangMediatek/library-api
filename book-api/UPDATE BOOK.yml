info:
  name: UPDATE BOOK
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/books/:id
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Test",
        "author": "GFROSsss"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
